import type { NextConfig } from "next";
const nextConfig: NextConfig = { output: "export", basePath: "/chelonakiwebsites", trailingSlash: true, images: { unoptimized: true } };
export default nextConfig;
